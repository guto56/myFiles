import os

if os.name == 'nt':
    os.system('cls')  # Limpa a tela no Windows
else:
    os.system('clear')  # Limpa a tela no Linux/Mac

produto = {
    'nome': 'Caneta Azul', 
    'preço': 2.5,
    'categoria': 'Escritório',
}

print(produto)